node_modules/
dist/
.env
.env.local
.DS_Store
Thumbs.db
.mcp.json

# Datos privados del negocio — NUNCA subir al repo (saldos/clientes reales)
*.xlsx
*.xls
*.csv

# Salidas de migración con datos reales (privadas, NUNCA al repo)
tools/migracion-*.json
tools/*.local.*

# Artefactos de los emuladores de Firebase (ruido local, no versionar)
*-debug.log
firebase-debug.log
firestore-debug.log
ui-debug.log
.firebase/

# Mockups efímeros del compañero visual de brainstorming (no versionar)
.superpowers/

# Permisos LOCALES del harness de Claude Code (por máquina, no versionar; CLAUDE.md §2).
# `settings.json` (compartido) SÍ se versiona; `settings.local.json` NO.
.claude/settings.local.json

