# ⚡ 10 — MEMORIA A CORTO PLAZO (WIP / Sprint activo)

> **Nodo neuronal: Memoria a Corto Plazo.** Junto con `CLAUDE.md` + `docs/05-ESTADO-GLOBAL.md`,
> es de las primeras lecturas de cada sesión (Ignorancia Selectiva, `CLAUDE.md §G`).
> Contiene solo lo vivo: foco actual, pendientes abiertos, bitácora. Estado técnico → `docs/05-ESTADO-GLOBAL.md`.
>
> **Es la pizarra, no el archivo.** Al cerrar una tarea: consolidar a ADR (`docs/99-HISTORIAL-ADR.md`) +
> fila en `docs/00-INDICE.md`, extraer lecciones a `docs/30-LECCIONES.md`, y PODAR esto al foco vivo (GC §G.4).

---

## 🎯 Foco actual
> 🎉 **CRM en producción + Reestructura Fase R desplegada (2026-06-06, ADR §47 + §49).** "Nuevo Bersaglio" vivo: Fase 1 (rediseño) + Fase 2 (hardening A/B) + Fase 3 (CRM cuentas por cobrar).
>
> **En prod ahora**: 344 clientes de Kary (cartera **$506.510.780**) + 12 pendientes · `recalcSaldoCliente` viva (Node 22 / ff v7) · sitio HTTP 200. **Fase R aplicada**: **solo Kary (admin) + Daniel (owner) operan**; las vendedoras NO tienen usuario → son entidad de datos (`vendedoras/{id}`, Kary las crea y asigna clientes); CRM admin-only; app de vendedora + rol + `solicitudesCorreccion` eliminados.
>
> **▶️ RETOMAR AQUÍ (cierre 2026-06-08) — PRÓXIMA SESIÓN, EN ESTE ORDEN (pedido explícito de Daniel):**
> 1. **🧠 MEJORAS AL CEREBRO primero** (TODO-16): al arrancar, **Daniel DESCRIBIRÁ mejoras al cerebro neuronal + a mis capacidades** (inteligencia/autonomía/otras habilidades). **Invítalo a describirlas**, diséñalas con él y aplícalas ANTES de seguir construyendo. Es lo primero del próximo chat.
> 2. **🔒 App Check — solo falta ENFORCEMENT** (ADR §54, TODO-14): el código YA está en prod y **VERIFICADO en vivo** (site key exacta `6LdSoxQt…Za3K` + reCAPTCHA v3 wired en público+admin; subagente lo confirmó en `js/chunks/firebase-config-9fcTmO9d.js`). El monitor mostró 0% verificadas = **propagación reciente + caché vieja, NO bug**. **Mañana**: Daniel re-mira el monitor (Firebase→App Check→pestaña **APIs**) → cuando **"verificadas" suba ~100%** del tráfico real → activar **Enforce** (Firestore+Storage+Functions) **JUNTOS** (1 clic, reversible). ⚠️ **NUNCA aplicar con 0% verificadas** (rompe el sitio). Guía paso a paso (Daniel no es técnico, pantallazos).
> 3. **🏗️ Seguir F6**: cimientos (CI rule-test TODO-10 + entero-COP) · reconciliación + vista Salud · RBAC custom claims · **backup PRE-1** (TODO-15, bloquea F7). Plan: `docs/superpowers/specs/2026-06-08-f6-hardening-plan.md`. Luego F7 (Consejo Externo antes).
>
> **Contexto Panel v2** (mini-ERP, IA "C", por fases; norte `docs/superpowers/specs/2026-06-07-bersaglio-arquitectura-maestra-design.md` v3): F-CHASIS-A §50 ✅ · Morosos/Vencidos §51 ✅ · F5 filtros §52 ✅ · F4 Bandeja §53 ✅ — **todo DESPLEGADO/mergeado a prod**. F6 §54 (App Check) en curso (arriba).
> - **Decisiones de morosos:** plazo 30 días (config) · `fecha` en movimientos nuevos (default hoy); migrados traen `fecha:CUTOFF`; sin fecha→`fechaCorteMigracion`→"Falta fecha" ámbar · VENCIDO día 1 pasado el plazo · rangos 1-30/31-60/+60 en rojo.
> - **TODO-17 (CRM linkage)**: contacto→Bandeja ✅; **newsletter** (`addSubscription`→`subscriptions`) NO se ve en el panel → conectar al CRM como mejora.
> - **Pendiente operativo**: smoke de panel por Kary; crear vendedoras reales.
>
> ⚠️ **Deploy (L-22 + L-26)**: reglas/functions = deploy manual; sitio + merge a `main` = PR que mergea Daniel en GitHub (`git fetch` siempre). Admin SDK = ADC (L-23).

---

## 📋 Pendientes abiertos (TODO-NN)

| ID | Item | Estado | Bloqueo |
|---|---|---|---|
| TODO-03 | (Opcional) Migrar headers de `99-HISTORIAL` a formato numerado `## NN.` para el offset-drift estricto del linter (hoy convención por fecha, válida y verde). | 🔲 Abierto | Baja prioridad |
| TODO-04 | (Opcional) Limpieza de anomalías 🔧 en `skills/`: cuarentenar `skill-creator/skill-creator/` (anidado redundante); `code-simplifier`/`code-modernization` son formatos subagente/plugin (no skill) — normalizar o dejar documentadas. | 🔲 Abierto | Baja prioridad |
| TODO-05 | Merge `Desarrollo → main` + deploy a producción. → **✅ PR #189 (`a04b1a3`) + deploy manual reglas/functions + migración Fase A**. CRM en prod. | ✅ Hecho | → ADR §47 |
| TODO-06 | **Commit del rediseño Fase 1** → commiteado `e290f83` + pusheado `origin/Desarrollo` (verif. 2026-06-05). | ✅ Hecho | — |
| TODO-07 | **Contenido real**: reseñas Google Maps (Nosotros), Films y feed Redes (Meta Graph / TikTok API). Marcado `TODO:` en `js/data/home-media.js` + `js/pages/nosotros.js`. | 🔲 Abierto | Cliente entrega datos/fuentes |
| TODO-08 | **Fase 2 — Hardening**: S1✅(código)+S3✅ aplicados (Tier A); pendiente CSP/reglas/claims (Tier B/C). Backlog+progreso → `docs/41-SEGURIDAD §1.5`. | 🟡 En curso | Tier B reglas = emulador+deploy gated |
| TODO-09 | **Fase 3 — CRM**: B1-B5 + editabilidad + **Reestructura Fase R** ✅ DESPLEGADOS (ADR §42–§49). Vendedoras = entidad (Kary las crea; ya no necesitan correo/usuario). Siguiente: **Fase M** (movimientos robustos, spec listo) + B6 reportes/atrasados. | 🟡 En curso (en prod ✅) | Fase M = nuevo plan |
| TODO-10 | **Reactivar CI rules-test**: poner `on: [push, pull_request]` en `.github/workflows/firestore-rules-test.yml` (la causa del rojo —bug S6— ya está resuelta). Verifica al pushear. | 🔲 Abierto | A pedido (requiere push) |
| TODO-11 | **Consejo Externo Panel v2** (Gemini 3.1 Pro). ✅ Corrido por Daniel + peer review aplicado → spec **v3** (§16). Cambios netos: saldo **síncrono** (no async), **sin backfill** de Money (COP ya entero exacto), **recompute O(M)** (no incremental), DIAN por **Adapter**. Pendiente: volcar a ADR `99` al cerrar F-CHASIS-A. | ✅ Hecho | → spec §16 |
| TODO-12 | **F-CHASIS-A ✅ DESPLEGADO** (Desarrollo→main PR #195, `2fef1fe`; Pages success; `.co` HTTP 200; rail v2 vivo). Consolidado en **ADR §50**. | ✅ Hecho + desplegado | → ADR §50 |
| TODO-13 | **DEPLOY COORDINADO** (PRE-integridad §13 + MOROSOS §51): reglas append-only + collectionGroup + `fecha` ISO + `onInquiryCreated` idempotente. → **✅ DESPLEGADO 2026-06-07** (PR #199 `0cfdb1d` front por Pages + `firebase deploy --only firestore:rules,functions`; smoke `functions:list` OK + `.co` 200). | ✅ Hecho | → ADR §51 |
| TODO-14 | **App Check (F6 §54)** — ✅ reCAPTCHA v3 registrado + DESPLEGADO + VERIFICADO EN VIVO (2026-06-08): un subagente confirmó en el bundle live `js/chunks/firebase-config-9fcTmO9d.js` la site key EXACTA `6LdSoxQt…Za3K` + `initializeAppCheck`/reCAPTCHA v3 (`recaptcha/api.js`, header `X-Firebase-AppCheck`) wired, guarda `isDev` ok, en público + admin. **PENDIENTE = solo ENFORCEMENT**: el monitor mostró 0% verificadas (propagación reciente de reCAPTCHA + clientes viejos en caché), NO un bug. Esperar horas/1 día → re-mirar monitor App Check (APIs) → cuando "verificadas" suba ~100% del tráfico real, activar **Enforce** (Firestore+Storage+Functions). ⚠️ NO aplicar con 0% verificadas (rompe el sitio). | 🟡 Código en prod OK; falta enforcement tras propagación | Daniel (enforce guiado) |
| TODO-15 | **Backup (PRE-1, Daniel)** — bloqueante antes de F7: elegir PITR pago vs export programado a Storage + lifecycle + **restore probado** (runbook). Nada nuevo de dinero (ventas/facturas) sin copia. | 🔲 Abierto | Acción de Daniel (consola) + decisión de presupuesto |
| TODO-17 | **Conectar TODA captura de contactos al CRM** (pedido por Daniel 2026-06-08): hoy el **formulario de contacto** → `inquiries` → Bandeja (F4) ✅; pero el **newsletter** (`addSubscription`→`subscriptions`, `firestore-service.js:544`) se guarda aparte y NO se ve en el panel. Mejora: traer suscripciones a la Bandeja (o vista propia) + revisar cualquier otra captura (reseñas son aparte). Que ningún contacto quede fuera del CRM. | 🔲 Abierto | Mejora tras App Check |
| TODO-16 | **🧠 PRÓXIMA SESIÓN (lo PRIMERO, antes de seguir F6): MEJORAS AL CEREBRO + CAPACIDADES** (pedido por Daniel 2026-06-08, reafirmado al cierre): Daniel **describirá** mejoras al cerebro neuronal + a la inteligencia/autonomía/otras habilidades del agente. Al arrancar el próximo chat: invitarlo a describirlas → diseñarlas → aplicarlas. NO arrancar a construir F6 sin antes hacer esto. | 🔲 Abierto · ALTA prioridad | Próxima sesión · Daniel describe |
| — | `.claude/settings.local.json` → **resuelto** (commit `e3d390f`): desindexado + gitignorado (config local del harness, no se versiona). | ✅ info | — |

> ✅ Cerrados y consolidados: **TODO-01 / TODO-02** → ADR §38 (commit `1be38d1`).

---

## 🔮 Contexto estratégico
Programa "Nuevo Bersaglio" (3 fases): **Fase 1** rediseño Liquid Glass ✅ · **Fase 2** hardening (Tier A/B ✅, Tier C pendiente — `docs/41-SEGURIDAD.md`) · **Fase 3** CRM de cuentas por cobrar ✅ **en producción con datos reales** (charter `docs/50-ARQUITECTURA.md`). Service Worker `bersaglio-v9`. Próximo horizonte: vendedoras + reportes/aging + (futuro) inventario/facturación.

---

## 📝 Bitácora (efímera — se vacía al consolidar)
- **2026-06-08 (CIERRE de sesión — App Check verificado, brain listo para próxima sesión)**: sesión larga y productiva (Morosos deploy + F5 + F4 + housekeeping settings.local + F6 App Check). App Check: site key (`6LdSoxQt…`) confirmada 3 veces (código + bundle live + Daniel). **Verificado en vivo por subagente**: el bundle `firebase-config-9fcTmO9d.js` tiene la key exacta + reCAPTCHA v3 wired (público+admin). Monitor 0% verificadas = propagación + caché, NO bug. **Próxima sesión (orden de Daniel)**: (1) MEJORAS AL CEREBRO (TODO-16, Daniel describe) → (2) App Check enforcement (re-mirar monitor → enforce juntos) → (3) seguir F6. brain:check SANO, repo limpio (1 commit cerebro `e4bf9b6` local sin pushear; Daniel lo incluye en su próximo push).
- **2026-06-08 (App Check REGISTRADO — guiado con Daniel)**: sesión guiada paso a paso (pantallazos) → Daniel registró reCAPTCHA v3 en Firebase App Check (app `bersaglio-web` "Registrada"; dominios `.co` + `.github.io`; proyecto bersaglio-jewelry; secret pegada en Firebase). Site key (PÚBLICA) `6LdSoxQtAAAAAAAiQ_dZz8-Cy5mARqjhCMtcZa3K` hardcodeada como fallback en `firebase-config.js` (como las llaves Firebase, L-14) → NO hace falta secret de GitHub. Build verde. **Falta**: merge a main + verificar monitor + activar Enforcement (en ese orden; enforcement antes del deploy rompería el sitio).
- **2026-06-08 (F6 inicio: App Check — ADR §54)**: Daniel eligió arrancar F6 por App Check (cerrar denial-of-wallet). Housekeeping: `.claude/settings.local.json` desindexado+gitignorado (`e3d390f`). Plan F6 grounded (workflow 4 agentes) → spec `2026-06-08-f6-hardening-plan.md`. **App Check código listo**: `initializeAppCheck` (reCAPTCHA v3) en `firebase-config.js`, gateado por `VITE_RECAPTCHA_SITE_KEY` + skip dev (no rompe sin key); cableado en `deploy.yml` + `.env.example`. Build verde. **Decisión de arquitecto**: App Check directo sobre Firestore (no reescribir forms a CF — eso es follow-up). Lección **L-30**. En `Desarrollo` sin mergear. **Bloquea en Daniel** (consola: reCAPTCHA + secret + enforcement; TODO-14). Siguiente F6: cimientos · reconciliación/Salud · RBAC claims · backup (TODO-15).
- **2026-06-07 (F4-leads Bandeja — ADR §53)**: pipeline de leads sobre `inquiries` (sin colección `leads` nueva → front+servicio, sin reglas/CF). 5 estados + origen + antigüedad/demorado + convertir-a-cliente (admin, `createCliente`) + alta manual + badge=nuevos. Helper puro `lead-format.js` (test:leads 8/8). Dashboard alineado (esPendiente + pill por estado). Revisión adversarial 2 lentes → fixes (convertir idempotente+best-effort, CSV pipeline, setStatus sync, convertido UI, label dashboard). Build verde, wiring 31/31 ids. En `Desarrollo` sin mergear. Gaps→F6 (reglas convertedTo, leads formal, App Check). Siguiente: F6 o F7.
- **2026-06-07 (F5 filtros — ADR §52)**: barra de filtros front-only sobre la lista CxC — chips de estado + rango de mora + select de vendedora + contador, combinables con búsqueda y orden por mora. Predicado puro `pasaFiltros`; reusa `.adm-filter-btn` (§3.2) + fix de doctrina (`transition:all`→props, `cursor`). Control de crédito DESCARTADO (Daniel: sin cupo por cliente; va con F7). Build verde. En `Desarrollo` sin mergear (front → Pages al mergear). Siguiente: F4-leads o F7.
- **2026-06-07 (MOROSOS/VENCIDOS — ADR §51)**: aging de cartera EN VIVO sin infra nueva. Helper PURO `js/crm-estado-cuenta.js` (FIFO, espejo de `saldo.js`; vencido + rangos 1-30/31-60/+60) + `fecha` real en movimientos (modal + `addMovimiento` + reglas ISO) + `onAllMovimientosChange` (listener collectionGroup, filter-free → sin índice) + ficha con sello de estado + lista CxC (KPI cartera vencida en rojo + orden por mora). `saldoActual` intacto (la mora deriva). TDD: `test:estado` 15/15. Revisión adversarial 3 lentes → fixes (fecha round-trip, formato ISO en reglas, listener en vivo, truncado audible, pill ámbar sin-fecha); rechazado "fechaCorte impide sinFecha" (fallback intencional). `test:saldo` 12/12 · `test:rules` 37/37 · build verde · brain:check SANO · sin cache bump. Lección **L-29**. **DESPLEGADO a prod 2026-06-07** (Daniel mergeó PR #199 → front por Pages; Claude `firebase deploy --only firestore:rules,functions`; smoke `functions:list` 6 vivas + `.co` 200). TODO-13 cerrado. Siguiente: **F5 completo** (filtros + control de crédito).
- **2026-06-07 (PRE-integridad, código — TODO-13)**: `movimientos` append-only (reglas `delete:if false` + update solo anular con `motivoAnulacion`, helper `anulacionValida`) + modal de anulación con motivo en la ficha + `onInquiryCreated` idempotente (`_counted` en la misma TX). Reglas 33/33 (emulador) + build verde. Commit `ea1f93f` (Desarrollo, sin desplegar). Pendiente: deploy coordinado (reglas+functions manual + front por Pages) + infra Daniel (backup, App Check). Siguiente: F1+F2 (aging) tras backup.
- **2026-06-07 (Panel v2 — diseño + F-CHASIS-A desplegado)** → consolidado en **ADR §50** (`99` + `00`/`05`/`20`/`50` + lecciones L-27/L-28): diseño maestro (mini-ERP, IA "C", spec v3) + Consejo Externo (Gemini 3.1 Pro, simplificó: saldo síncrono, sin backfill, O(M), DIAN por Adapter) + F-CHASIS-A construido por subagentes y **desplegado a prod** (`main` PR #195). Siguiente: **F1** (`estadoCuenta`).
- **2026-06-06 (CRM Reestructura Fase R, ADR §49)**: vendedora = dato (no usuario); CRM admin-only; app de vendedora + rol + `solicitudesCorreccion` ELIMINADOS; nueva colección `vendedoras` + gestión en Configuración + `vendedoraId`. Ejecutada con subagentes (6 tareas TDD, doble revisión spec/calidad por tarea). Reglas 29/29 + saldo 12/12 + integración 5/5; build verde. Desplegado: reglas+functions (manual) + sitio (PR #191 de Daniel). 344 clientes intactos; smoke prod OK (colección vendedoras write/read/delete). L-26. Siguiente: **Fase M** (movimientos robustos, spec listo).
- **2026-06-06 (Mantenimiento — upgrade runtime functions, ADR §48)**: Node 20→22 + firebase-functions 6.6→7.2.5 + firebase-admin 13.7→13.10 en `functions/package.json`. Cero cambios de código (la API v2 no cambió en v7; verificado por release oficial + package instalado + tests). `test:saldo` 12/12 + integración 5/5 con v7. Deploy → 6 functions en nodejs22; smoke test en prod OK (`recalcSaldoCliente` recalcula); 344 clientes intactos. Rama `chore/upgrade-functions-v7-node22` mergeada a Desarrollo. L-25. Deuda Node 20 de §47 resuelta.
- **2026-06-06 (LANZAMIENTO CRM, ADR §47)**: Audit de preparación read-only (workflow 4 agentes) → 3 correcciones al cerebro (`main` ya tenía el CRM; `firebase-deploy.yml` es Hosting-only; `recalcSaldoCliente` no estaba en prod). Deploy manual reglas+índices+functions (`recalcSaldoCliente` viva) + merge vía **PR #189** + ADC (`gcloud auth application-default login` + quota project) + **migración Fase A 345/345 exacto** → se borró 1 fila "TOTAL" basura (L-24) → **344 clientes, cartera $506.510.780**, 12 pendientes. Extractor parchado (`NON_CLIENT_RE`). Sitio HTTP 200. Lecciones L-22/23/24.
- _Historial previo (cerebro v1.0.0, Fase 1 rediseño+pulido, Fase 2 hardening, CRM B1-B5, verificación E2E) ya consolidado en ADRs §37–§46 — ver `00-INDICE`._
