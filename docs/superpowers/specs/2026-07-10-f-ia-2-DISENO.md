# F-IA-2 — Panel coherente · SPEC DE IMPLEMENTACIÓN para el interinato Opus 4.8 [FABLE-5]

> **Diseñada por Fable 5 (titular) el 2026-07-10 con el 7% final de su cuota** — todas las
> decisiones de fondo YA están tomadas aquí; Opus implementa mecánicamente bajo el protocolo
> `opus-interino-protocolo` (R1-R7 VINCULANTES). Norte: plan maestro v5 §3 + minería §183
> (principios twenty T-NN). Madre: `2026-07-10-plan-maestro-v5-ia-tesoreria.md`.
> **Fable audita esto al volver (jueves tras reset de cuota) con `git log --grep="[OPUS-4.8]"`.**

## 0. Reglas del interinato para ESTA spec
- Cada bloque = commits atómicos marcados `[OPUS-4.8]` + tests + build + **validación Chrome
  HOLÍSTICA** (`[[feedback_validacion_chrome_holistica]]`: barrer TODA la pantalla, no túnel).
- **CERO lógica nueva de dinero en `functions/`** en toda F-IA-2 (es re-organización de UI +
  lecturas). Si un bloque parece necesitar una CF nueva → STOP, dejarlo documentado en la cola
  de Fable (R5/R6). Excepción única permitida: índices de Firestore (`firestore.indexes.json`
  + deploy manual `firebase deploy --only firestore:indexes`, L-22).
- **Zonas calientes NO TOCAR** (R3): `functions/` (webhook Wompi, firma, reaper, snapshot,
  caja-core, pedidos-core), `firestore.rules` (salvo que un bloque lo pida EXPLÍCITO aquí — ninguno
  lo pide), checkout público, `wompi-core`.
- Dinero en pantalla: formateadores SIN clamp (L-26 §181: anomalía negativa se MUESTRA en rojo,
  jamás se recorta a $0). Reusar `fmtCOPCompacto`/`setStatMoney`/aging-bar de `js/admin/cuentas.js`
  (§182) y `.adm-money`/`saldo-format.js` — no inventar formateadores nuevos.
- Al cambiar el shell del panel (sidebar/rail o JS compartido): bump MAYOR de `CACHE_NAME` en
  `public/sw.js` + `APP_VERSION` + actualizar `05` (§4). Un solo bump al final de cada bloque
  desplegado, no por commit.
- Deploy: build/tests verdes → push `Desarrollo` → merge a `main` (Pages). Functions NO se tocan.

## 0.5 Visión final y MARCO DE DEPURACIÓN (feedback de Daniel 2026-07-10 — VINCULANTE)
> Daniel, tras el rail v2: *"no vi más que cambios de nombre... tenemos menús regadas, funciones
> regadas, nada compacto que pueda entender un usuario normal como Kary al capacitarlo, ni yo
> mismo lo entiendo en muchos aspectos"*. → `[[feedback_reorganizar_no_es_depurar]]`.
> F-IA-2 NO es renombrar: es DEPURAR el contenido de cada página contra la visión final.

**Visión final**: un panel que Kary opera SIN haber visto su construcción y que Daniel entiende
al abrirlo. Cada elemento de cada página pasa 4 preguntas-gate:
1. ¿Lo usa Kary (diario/semanal) o Daniel (supervisión)? Si nadie → residuo.
2. ¿Se entiende sin capacitación? (es-CO, cero jerga; dinero: *qué pasó + qué pasó con la plata + qué hacer*).
3. ¿Apunta a una fase del plan v5 o es andamiaje de construcción/migración? Andamiaje → fuera de la UI.
4. ¿Su información vive UNA sola vez? Duplicado → fusionar.
**Estados vacíos pre-lanzamiento HONESTOS**: jamás mostrar cifras/actas sin sentido ("vencido $0",
actas de meses sin actividad); en su lugar: "Aún no hay actividad real este mes".
Veredictos posibles por elemento: **SIRVE** (queda) · **FUSIONAR** · **REESCRIBIR** (microcopy) ·
**ELIMINAR** (residuo). Lo estructural/dinero que no tenga veredicto aquí → cola de Fable, NO improvisar.

## 1. Orden de ejecución (riesgo ascendente)
`B0 Inventario+depuración (recorrido Chrome)` → `B1 Parámetros unificados` → `B2 Split Clientes/Cartera` → `B3 "Hoy" con dinero` → `B4 Bandeja de aprobaciones` → `B5 Salud legible + microcopy global`.
Cada bloque es entregable independiente (si la cuota/semana se corta, lo hecho queda íntegro).

## 1.5 B0 — Inventario página-por-página (la "revisión de la plataforma" que pidió Daniel)
**Qué**: recorrer en Chrome CADA página del rail (holístico) levantando inventario: secciones,
funciones, botones, textos. Contrastar cada elemento con los veredictos YA DECIDIDOS abajo; todo
elemento SIN veredicto se anota en una tabla "Hallazgos B0" al final de esta spec con propuesta
(SIRVE/FUSIONAR/REESCRIBIR/ELIMINAR) — los de UI se ejecutan en B1-B5; los de dinero/estructura
van a la cola de Fable §6. **Veredictos ya decididos por el titular**:
| Página | Elemento | Veredicto |
|---|---|---|
| Negocio y equipo (config) | Bloque "Migración del Kardex" | **ELIMINAR** de la UI (no habrá más migraciones; Kary cargará manual; runbook queda en el cerebro) |
| Negocio y equipo | Tablero "Pendientes por definir/completar" (`pendientes`) | **ELIMINAR** de la UI (era andamiaje del setup de migración; la colección muere en la limpieza pre-lanzamiento) |
| Sistema (rail) | Páginas Configuración + Parámetros + Usuarios | **FUSIONAR** en UNA página con pestañas (B1); rail Sistema queda con SOLO 2 items: "Negocio y equipo" + "Salud del sistema" |
| Salud del sistema | "Registro de fallos" y semáforos | **REESCRIBIR** en lenguaje claro (B5) |
| Cartera | "Último corte inmutable" + "Acta mensual de conciliación" (auditoría Fase M) | **FUSIONAR→Salud** como sección "Cierre de mes" reescrita + estado vacío honesto (B5); la página Cartera queda SOLO operativa (vencidos, acuerdos, mora) |
| Mostrador (POS) | Export al contador | **SIRVE** (se muda recién en F-REPORTES, plan v5 §3.5 — no tocar ahora) |
| Pedidos / Interesados / Bóveda / Caja | contenido actual | **SIRVE** (depurado en §176-§182); solo microcopy B5 si algún texto falla la pregunta 2 |
**Gate B0**: tabla de inventario completa en esta spec (evidencia: commit `[OPUS-4.8]`) ANTES de
empezar B1. Es el mapa que evita "colocar por colocar".

## 2. B1 — Parámetros unificados (fusión Configuración + Parámetros)
**Qué**: UNA sola página de configuración con pestañas `Negocio · Cobranza · Caja · Fiscal · Equipo`.
Mata la duplicación de cobranza (hallazgo A3 §182) y retira "Migración del Kardex" de la UI.
- Página superviviente: **`admin-config.html` + `js/admin/config.js`** (título de página == item del
  rail == breadcrumb: **"Negocio y equipo"**). La página "Parámetros" actual desaparece del rail
  (`js/admin/sidebar-data.js`): verificar su href real en `NAV` y eliminar el item; su HTML queda
  (link muerto NO — añadir `<meta http-equiv="refresh">` a config o script redirect, decisión ya
  tomada: redirect JS con aviso).
- Pestañas = tabs client-side (sin router): secciones ya existentes se REAGRUPAN, no se reescriben.
  Mapeo (6 pestañas): Negocio=`config/negocio`+identidad · Cobranza=fecha de corte+días plazo+params
  de mora (UNA sola fuente; el duplicado de la página Parámetros se elimina) · Caja=`config/caja`
  (enforceTurno, limiteCajon — SOLO lectura/edición de los campos que ya edita hoy; sin campos
  nuevos) · Fiscal=`config/fiscal` **pestaña visible SOLO owner** (§181m: reglas ya lo fuerzan
  server-side; la UI lo respeta con `hasRole('owner')`) · Equipo=Vendedoras (CRUD actual) ·
  **Usuarios** (pestaña SOLO owner: absorbe la página de gestión de usuarios actual — mismo código,
  mismas callables `createUser`/`updateUserRole`/`deactivateUser`, mismo gate `requireAuth('owner')`;
  su página vieja sale del rail con redirect).
- **ELIMINAR de la UI** (B0, orden de Daniel): bloque "Migración del Kardex" (one-time ya aplicado;
  runbook en el cerebro) y tablero "Pendientes por definir/completar" (andamiaje del setup; la
  colección `pendientes` muere en la limpieza pre-lanzamiento — solo se retira la UI, sin borrar datos).
- **Gate B1**: `npm run test:sidebar` (ajustar fixture del rail) + build + Chrome: cada pestaña
  carga, guarda y relee su doc real; pestaña Fiscal invisible como admin (probar con rol admin
  simulado o gate en código revisado); redirect de la página vieja funciona.

## 3. B2 — Split Clientes (directorio) / Cartera (dinero)
**Qué**: hallazgo A1 §182 — "Clientes" hoy abre la página de cartera. Se separan.
- **NUEVO `admin-clientes.html` + `js/admin/clientes.js`** = DIRECTORIO: lista/búsqueda de
  clientes (nombre, teléfono, vendedora), cumpleaños del mes (link WhatsApp), modal nuevo
  cliente (lo MUEVE de `cuentas.js`), fila → ficha (`admin-cuenta.html`). SIN saldos ni aging
  (un chip de saldo por fila SÍ, reusando `saldoCellHTML` — da contexto sin convertirla en cartera).
  Datos: reusar `js/crm-service.js` tal cual (cero cambios de contrato).
- **`admin-cuentas.html`** queda como **CARTERA** (título "Cartera"): KPIs (§182: abreviado +
  exacto + mini-barra aging), vencidos, acuerdos, filtros de mora, orden por mora. Se le QUITA
  el modal de nuevo cliente y el bloque cumpleaños (migran al directorio).
- **Rail** (`sidebar-data.js`): grupo Clientes → items `Clientes` (nuevo html) e `Interesados`;
  grupo NUEVO **`Cartera`** → item `Cartera` (admin-cuentas.html). Roles: iguales a los actuales
  de Cuentas (admin/owner). menú==título==breadcrumb en las 2 páginas.
- **Enlaces bidireccionales** (twenty T-13, §183): ficha muestra chip "Ver en Cartera" (filtro por
  cliente) y Cartera → click abre la ficha. La ficha (`admin-cuenta.html`) NO se reestructura en
  pestañas todavía (eso queda para Fable con la ficha-timeline T-5; NO hacerlo en el interinato).
- **Gate B2**: build + `test:sidebar` + Chrome holístico: alta de cliente desde el directorio →
  aparece en vivo y al recargar; cartera sigue cuadrando (mismos números que antes del split —
  invariante mismo-número-en-todas-las-vistas de `auditoria-financiera`); estado-cero del
  directorio con búsqueda sin resultados.

## 4. B3 — "Hoy" con dinero (pulso del negocio, máx 5 señales)
**Qué**: hallazgo A5 §182. La página del grupo "Hoy" (verificar href real en `sidebar-data.js`)
gana el pulso. Patrón twenty T-24 (§183): cada señal = widget autónomo `{query → agregado → render}`
en `js/admin/hoy.js`; una fila de tarjetas + lista corta. TODO read-only (listeners), CERO escrituras.
1. **Vendido hoy**: $ y # de `pedidos` del día (estados pagado/entregado; reusar censo de estados
   de `pedidos-format.js` — NO redefinir qué cuenta como vendido).
2. **Turno/efectivo**: turno abierto (operadora, fondo, estimado del cajón — MISMOS acumuladores
   del doc del turno §181, sin recalcular nada en cliente).
3. **Quién paga hoy**: cuotas de acuerdos que vencen hoy/esta semana con NOMBRES (helper
   `crm-estado-cuenta.js` `plan` — reusar, no duplicar FIFO).
4. **Pedidos por entregar**: # + lista corta → deep-link `admin-pedidos.html?id=`.
5. **Avisos**: aprobaciones pendientes (mismo contador del B4) + `saludEventos` sin resolver.
- **Vista Daniel (owner)**: fila extra con plata total visible por rol (suma de: estimado cajón +
  bóveda) + "¿cuadró el último arqueo?" (último doc de `arqueo`: descuadre $0 verde / ≠0 ROJO).
  ⚠️ SIN clamp: descuadre negativo se pinta tal cual.
- Si alguna query pide índice compuesto → `firestore.indexes.json` + deploy manual de índices SOLO.
- **Gate B3**: build + Chrome holístico con datos reales de prod (los números del Hoy == los de
  sus páginas fuente: POS, Cartera, Pedidos — invariante mismo-número; probar también con turno
  CERRADO = estado-cero limpio).

## 5. B4 — Bandeja única de aprobaciones (la más delicada — dinero visible)
**Qué**: hallazgo A4 §182. Patrón twenty T-25 (§183): la bandeja NO es un módulo nuevo — es una
VISTA agregadora de lo pendiente que YA existe, con las acciones EXISTENTES.
- **NUEVO `admin-aprobaciones.html` + `js/admin/aprobaciones.js`**, rail: item `Aprobaciones` en
  grupo Finanzas. Lee y agrega SOLO fuentes existentes con estado pendiente-de-aprobación:
  reversas/movimientos de bóveda pendientes + reversas de caja pendientes (verificar nombres
  exactos de colecciones/estados en `js/admin/boveda.js` y `js/admin/pos.js`/`caja-format.js` —
  leer el código ANTES, §3.3). Cada fila: qué es + monto + quién lo pidió + cuándo + link al
  contexto de origen.
- **Acciones**: botones Aprobar/Rechazar llaman las **callables EXISTENTES** (las mismas que hoy
  usan Bóveda/POS — localizar por nombre en el código; PROHIBIDO crear callables nuevas o alterar
  firmas). Owner ve botones; admin ve la fila con "Esperando aprobación de Daniel".
- **Badge en el rail**: contador vivo de pendientes sobre el item `Aprobaciones` (extender
  `render-sidebar.js`/`sidebar-data.js` con soporte `badge` — cierra el pend "badge sidebar"
  §165-171). Test en `render-sidebar.test.mjs`.
- **Página de origen**: donde hoy se dispara una solicitud (bóveda), mostrar estado "Esperando
  aprobación" si ya existe — verificar si ya lo hace; si ya lo hace, NO tocar.
- **Gate B4 (doble, es dinero)**: tests de sidebar + cualquier helper nuevo puro con test propio +
  Chrome holístico del CICLO COMPLETO en prod: crear reversa → aparece en bandeja + badge →
  aprobar como owner → desaparece + el efecto neteo se refleja en Bóveda/turno (mismos números
  que el flujo viejo) → rechazar otra → estado terminal limpio. Si CUALQUIER paso exige lógica
  server nueva → STOP y a la cola de Fable (R5).

## 5.5 B5 — Salud legible + "Cierre de mes" + pasada de microcopy GLOBAL
**Qué**: que Daniel y Kary entiendan TODO lo que ven (hallazgos directos de Daniel 2026-07-10).
- **Salud del sistema** (`admin-salud.html`/`salud.js`) — reescritura de textos, misma mecánica:
  · Semáforos con nombre humano: "Copia de seguridad diaria" · "Cuadre de saldos (el sistema
    re-verifica que cada saldo coincida con sus movimientos)" · "Procesos automáticos".
  · "Registro de fallos" → **"Avisos del sistema"** + subtítulo: *"Si un proceso automático falla
    o un saldo no cuadra, aparece aquí para revisarlo. Vacío = todo en orden."* Cada fila: qué
    pasó (sin jerga) + qué hacer (botón). Estado vacío: "✅ Sin avisos — todo en orden".
- **"Cierre de mes" (nueva sección en Salud)**: mover aquí el "Último corte inmutable" y el
  "Acta mensual de conciliación" que hoy viven en la vista de cartera (localizar en
  `js/admin/auditoria-cartera.js`/`auditoria.js` — leer ANTES de mover). Reescribir: *"Al cerrar
  cada mes, el sistema guarda una foto de la cartera y verifica que el banco y el efectivo
  cuadren con lo registrado."* Estado vacío honesto: si el mes no tuvo actividad real →
  *"2026-06 no tuvo movimientos reales (datos de prueba/migración). El primer cierre con
  actividad aparecerá aquí."* — NUNCA actas con cifras sin contexto. La página Cartera queda
  SOLO operativa. ⚠️ Solo se MUEVE la presentación: la mecánica de cortes/actas (Fase M) NO se toca.
- **Pasada de microcopy GLOBAL** (todas las páginas del rail, con el inventario B0 en mano):
  cada título de sección, KPI, botón y aviso pasa la pregunta 2 del marco §0.5. Cambios SOLO de
  texto/labels (cero lógica). Dinero siempre: qué pasó + qué pasó con la plata + qué hacer.
- **Gate B5**: Chrome holístico de Salud + Cartera (la mecánica intacta: mismos datos, nueva
  presentación) + lectura en voz de usuario: cada pantalla debe poder explicársela Kary a una
  vendedora sin ayuda. Cualquier texto que Opus no sepa reescribir con certeza → lista para Fable.

## 6. Cola del TITULAR (Fable, al volver — NO tocar en interinato)
1. **Auditoría del interinato #3** (protocolo §4: grep marcador + suites dinero + Chrome).
2. **F-TESORERIA** (Decisión Fuerte de dinero: spec plan v5 §5 + skill `auditoria-financiera`
   COMPLETA + comité; ledger nuevo = diseño+implementación del titular).
3. **F-COMPRAS** (ídem, plan v5 §4) → **F-REPORTES** (plan v5 §6).
4. **F2.4 apartados** (TODO-39): implementación cuando Daniel cierre consejo externo + abogado.
5. Ficha de cliente en pestañas + timeline `actividades` (twenty T-5/T-13, §183).
6. Evaluación n8n (WhatsApp + reseñas) — Decisión Fuerte de infra, con Daniel.

## Checklist (marcar con evidencia: commit + test + validación)
- [ ] B0 Inventario página-por-página + tabla Hallazgos B0 en esta spec — commit `[OPUS-4.8]`: ______
- [ ] B1 Parámetros unificados (6 pestañas; Kardex+Pendientes ELIMINADOS; Usuarios absorbida) — commit: ______ · test:sidebar: ______ · Chrome: ______
- [ ] B2 Split Clientes/Cartera — commit: ______ · tests: ______ · Chrome (alta + cuadre): ______
- [ ] B3 Hoy con dinero — commit: ______ · Chrome (paridad de números + estado-cero): ______
- [ ] B4 Bandeja aprobaciones — commit: ______ · tests: ______ · Chrome (ciclo completo prod): ______
- [ ] B5 Salud legible + Cierre de mes movido + microcopy global — commit: ______ · Chrome: ______
- [ ] SW/APP_VERSION bump por bloque desplegado + `05` actualizado: ______
- [ ] Bitácora de interinato al cierre de CADA sesión (R7): ______

## Hallazgos B0 (la llena Opus durante el recorrido — elementos SIN veredicto previo)
| Página | Elemento | Qué es/hace | Propuesta | Destino (B1-B5 / cola Fable) |
|---|---|---|---|---|
| _(pendiente del recorrido)_ | | | | |
